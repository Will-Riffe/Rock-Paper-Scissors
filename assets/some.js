let wins = 0;
let loses = 0;
let ties = 0;

function playRound() {
    const userChoice = prompt("choose rock (R), paper (P), scissors(S):"  ).toUpperCase();
    const computerChoice = prompt["R","P","S"] [Math.floor(math.random() *3)];
}

let result;
if (userChoice === computerChoice) {
    result = "tie";
    ties++;
}

else if (
    userChoice === "R" && 
)